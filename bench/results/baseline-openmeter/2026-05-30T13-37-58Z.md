# Benchmark: baseline-openmeter

| Field | Value |
| --- | --- |
| Harness commit | `2a8c9f5-dirty` |
| ClickHouse | 26.2.19.43 (single-node) |
| Clusters | default |
| Sweep id | `20260530T133758Z` |
| Concurrency | 1 |
| Started | 2026-05-30T13:37:58Z |
| Finished | 2026-05-30T13:39:54Z |

## Ingest

Source `go-seeder`: 10000000 rows in 97.72s (102333 events/sec), batch=10000.

## Queries

| Query | p50 ms | p95 ms | p99 ms | QPS | CPU p50 ms | CPU src | Mem MB |
| --- | ---: | ---: | ---: | ---: | ---: | --- | ---: |
| agent_runs_by_name | 7.0 | 16.0 | 16.0 | 50.3 | 20.6 | os_cpu | 22.1 |
| avg_hour | 63.0 | 65.0 | 65.0 | 13.3 | 155.9 | os_cpu | 31.5 |
| count_hour | 12.0 | 12.0 | 12.0 | 43.4 | 28.8 | os_cpu | 9.5 |
| kong_status_by_route | 60.0 | 61.0 | 61.0 | 14.1 | 156.2 | os_cpu | 50.3 |
| latest_hour | 65.0 | 66.0 | 66.0 | 13.2 | 160.1 | os_cpu | 31.2 |
| llm_tokens_by_model | 45.0 | 46.0 | 46.0 | 17.5 | 125.1 | os_cpu | 57.3 |
| max_hour | 62.0 | 71.0 | 71.0 | 13.3 | 154.2 | os_cpu | 29.2 |
| min_hour | 64.0 | 68.0 | 68.0 | 13.2 | 159.6 | os_cpu | 30.2 |
| sum_day | 65.0 | 70.0 | 70.0 | 13.0 | 160.1 | os_cpu | 27.6 |
| sum_hour | 65.0 | 67.0 | 67.0 | 13.0 | 161.4 | os_cpu | 28.5 |
| sum_hour_group1 | 54.0 | 60.0 | 60.0 | 15.1 | 143.1 | os_cpu | 25.4 |
| sum_hour_group1_group2 | 61.0 | 65.0 | 65.0 | 13.5 | 168.2 | os_cpu | 27.7 |
| sum_hour_group1_group2_no_prewhere | 63.0 | 77.0 | 77.0 | 13.0 | 177.7 | os_cpu | 26.4 |
| sum_hour_group1_no_prewhere | 55.0 | 56.0 | 56.0 | 15.1 | 143.6 | os_cpu | 26.1 |
| sum_hour_tz | 65.0 | 75.0 | 75.0 | 12.9 | 160.6 | os_cpu | 29.1 |
| sum_minute | 78.0 | 82.0 | 82.0 | 10.9 | 206.5 | os_cpu | 47.9 |
| sum_month | 69.0 | 72.0 | 72.0 | 12.5 | 167.4 | os_cpu | 27.8 |
| sum_no_window | 61.0 | 65.0 | 65.0 | 13.6 | 152.5 | os_cpu | 28.7 |
| unique_count_hour | 58.0 | 61.0 | 61.0 | 14.2 | 165.7 | os_cpu | 72.5 |
| workload_seconds_by_region | 15.0 | 18.0 | 18.0 | 36.8 | 44.9 | os_cpu | 40.8 |

<details>
<summary><code>agent_runs_by_name</code></summary>

```sql
SELECT
  tumbleStart(om_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(om_events.time, toIntervalHour(1), 'UTC') AS windowend,
  count(*) AS value,
  JSON_VALUE(om_events.data, '$.agent_name') AS agent_name
FROM om_events
WHERE om_events.namespace = {namespace:String}
  AND om_events.type = 'agent_run'
  AND om_events.subject IN {subjects:Array(String)}
  AND om_events.time >= {from:DateTime}
  AND om_events.time < {to:DateTime}
GROUP BY windowstart, windowend, agent_name
ORDER BY windowstart
```

</details>

<details>
<summary><code>avg_hour</code></summary>

```sql
SELECT
  tumbleStart(om_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(om_events.time, toIntervalHour(1), 'UTC') AS windowend,
  avg(toDecimal128OrNull(JSON_VALUE(om_events.data, '$.value'), 19)) AS value,
  om_events.subject AS subject
FROM om_events
WHERE om_events.namespace = {namespace:String}
  AND om_events.type = {type:String}
  AND om_events.subject IN {subjects:Array(String)}
  AND om_events.time >= {from:DateTime}
  AND om_events.time < {to:DateTime}
GROUP BY windowstart, windowend, subject
ORDER BY windowstart
```

</details>

<details>
<summary><code>count_hour</code></summary>

```sql
SELECT
  tumbleStart(om_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(om_events.time, toIntervalHour(1), 'UTC') AS windowend,
  count(*) AS value,
  om_events.subject AS subject
FROM om_events
WHERE om_events.namespace = {namespace:String}
  AND om_events.type = {type:String}
  AND om_events.subject IN {subjects:Array(String)}
  AND om_events.time >= {from:DateTime}
  AND om_events.time < {to:DateTime}
GROUP BY windowstart, windowend, subject
ORDER BY windowstart
```

</details>

<details>
<summary><code>kong_status_by_route</code></summary>

```sql
SELECT
  tumbleStart(om_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(om_events.time, toIntervalHour(1), 'UTC') AS windowend,
  count(*) AS value,
  JSON_VALUE(om_events.data, '$.response_http_status') AS status,
  JSON_VALUE(om_events.data, '$.route_name') AS route
FROM om_events
WHERE om_events.namespace = {namespace:String}
  AND om_events.type = 'kong_api_request'
  AND om_events.subject IN {subjects:Array(String)}
  AND om_events.time >= {from:DateTime}
  AND om_events.time < {to:DateTime}
GROUP BY windowstart, windowend, status, route
ORDER BY windowstart
```

</details>

<details>
<summary><code>latest_hour</code></summary>

```sql
SELECT
  tumbleStart(om_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(om_events.time, toIntervalHour(1), 'UTC') AS windowend,
  argMax(toDecimal128OrNull(JSON_VALUE(om_events.data, '$.value'), 19), om_events.time) AS value,
  om_events.subject AS subject
FROM om_events
WHERE om_events.namespace = {namespace:String}
  AND om_events.type = {type:String}
  AND om_events.subject IN {subjects:Array(String)}
  AND om_events.time >= {from:DateTime}
  AND om_events.time < {to:DateTime}
GROUP BY windowstart, windowend, subject
ORDER BY windowstart
```

</details>

<details>
<summary><code>llm_tokens_by_model</code></summary>

```sql
SELECT
  tumbleStart(om_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(om_events.time, toIntervalHour(1), 'UTC') AS windowend,
  sum(toDecimal128OrNull(JSON_VALUE(om_events.data, '$.tokens'), 19)) AS value,
  JSON_VALUE(om_events.data, '$.model') AS model,
  JSON_VALUE(om_events.data, '$.provider') AS provider
FROM om_events
WHERE om_events.namespace = {namespace:String}
  AND om_events.type = 'llm_request'
  AND om_events.subject IN {subjects:Array(String)}
  AND om_events.time >= {from:DateTime}
  AND om_events.time < {to:DateTime}
GROUP BY windowstart, windowend, model, provider
ORDER BY windowstart
```

</details>

<details>
<summary><code>max_hour</code></summary>

```sql
SELECT
  tumbleStart(om_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(om_events.time, toIntervalHour(1), 'UTC') AS windowend,
  max(toDecimal128OrNull(JSON_VALUE(om_events.data, '$.value'), 19)) AS value,
  om_events.subject AS subject
FROM om_events
WHERE om_events.namespace = {namespace:String}
  AND om_events.type = {type:String}
  AND om_events.subject IN {subjects:Array(String)}
  AND om_events.time >= {from:DateTime}
  AND om_events.time < {to:DateTime}
GROUP BY windowstart, windowend, subject
ORDER BY windowstart
```

</details>

<details>
<summary><code>min_hour</code></summary>

```sql
SELECT
  tumbleStart(om_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(om_events.time, toIntervalHour(1), 'UTC') AS windowend,
  min(toDecimal128OrNull(JSON_VALUE(om_events.data, '$.value'), 19)) AS value,
  om_events.subject AS subject
FROM om_events
WHERE om_events.namespace = {namespace:String}
  AND om_events.type = {type:String}
  AND om_events.subject IN {subjects:Array(String)}
  AND om_events.time >= {from:DateTime}
  AND om_events.time < {to:DateTime}
GROUP BY windowstart, windowend, subject
ORDER BY windowstart
```

</details>

<details>
<summary><code>sum_day</code></summary>

```sql
SELECT
  tumbleStart(om_events.time, toIntervalDay(1), 'UTC') AS windowstart,
  tumbleEnd(om_events.time, toIntervalDay(1), 'UTC') AS windowend,
  sum(toDecimal128OrNull(JSON_VALUE(om_events.data, '$.value'), 19)) AS value,
  om_events.subject AS subject
FROM om_events
WHERE om_events.namespace = {namespace:String}
  AND om_events.type = {type:String}
  AND om_events.subject IN {subjects:Array(String)}
  AND om_events.time >= {from:DateTime}
  AND om_events.time < {to:DateTime}
GROUP BY windowstart, windowend, subject
ORDER BY windowstart
```

</details>

<details>
<summary><code>sum_hour</code></summary>

```sql
SELECT
  tumbleStart(om_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(om_events.time, toIntervalHour(1), 'UTC') AS windowend,
  sum(toDecimal128OrNull(JSON_VALUE(om_events.data, '$.value'), 19)) AS value,
  om_events.subject AS subject
FROM om_events
WHERE om_events.namespace = {namespace:String}
  AND om_events.type = {type:String}
  AND om_events.subject IN {subjects:Array(String)}
  AND om_events.time >= {from:DateTime}
  AND om_events.time < {to:DateTime}
GROUP BY windowstart, windowend, subject
ORDER BY windowstart
```

</details>

<details>
<summary><code>sum_hour_group1</code></summary>

```sql
SELECT
  tumbleStart(om_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(om_events.time, toIntervalHour(1), 'UTC') AS windowend,
  sum(toDecimal128OrNull(JSON_VALUE(om_events.data, '$.value'), 19)) AS value,
  om_events.subject AS subject,
  JSON_VALUE(om_events.data, '$.group1') AS group1
FROM om_events
WHERE om_events.namespace = {namespace:String}
  AND om_events.type = {type:String}
  AND om_events.subject IN {subjects:Array(String)}
  AND om_events.time >= {from:DateTime}
  AND om_events.time < {to:DateTime}
  AND JSON_VALUE(om_events.data, '$.group1') = {group1:String}
GROUP BY windowstart, windowend, subject, group1
ORDER BY windowstart
SETTINGS optimize_move_to_prewhere = 1, allow_reorder_prewhere_conditions = 1
```

</details>

<details>
<summary><code>sum_hour_group1_group2</code></summary>

```sql
SELECT
  tumbleStart(om_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(om_events.time, toIntervalHour(1), 'UTC') AS windowend,
  sum(toDecimal128OrNull(JSON_VALUE(om_events.data, '$.value'), 19)) AS value,
  om_events.subject AS subject,
  JSON_VALUE(om_events.data, '$.group1') AS group1,
  JSON_VALUE(om_events.data, '$.group2') AS group2
FROM om_events
WHERE om_events.namespace = {namespace:String}
  AND om_events.type = {type:String}
  AND om_events.subject IN {subjects:Array(String)}
  AND om_events.time >= {from:DateTime}
  AND om_events.time < {to:DateTime}
  AND JSON_VALUE(om_events.data, '$.group1') = {group1:String}
  AND JSON_VALUE(om_events.data, '$.group2') = {group2:String}
GROUP BY windowstart, windowend, subject, group1, group2
ORDER BY windowstart
SETTINGS optimize_move_to_prewhere = 1, allow_reorder_prewhere_conditions = 1
```

</details>

<details>
<summary><code>sum_hour_group1_group2_no_prewhere</code></summary>

```sql
SELECT
  tumbleStart(om_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(om_events.time, toIntervalHour(1), 'UTC') AS windowend,
  sum(toDecimal128OrNull(JSON_VALUE(om_events.data, '$.value'), 19)) AS value,
  om_events.subject AS subject,
  JSON_VALUE(om_events.data, '$.group1') AS group1,
  JSON_VALUE(om_events.data, '$.group2') AS group2
FROM om_events
WHERE om_events.namespace = {namespace:String}
  AND om_events.type = {type:String}
  AND om_events.subject IN {subjects:Array(String)}
  AND om_events.time >= {from:DateTime}
  AND om_events.time < {to:DateTime}
  AND JSON_VALUE(om_events.data, '$.group1') = {group1:String}
  AND JSON_VALUE(om_events.data, '$.group2') = {group2:String}
GROUP BY windowstart, windowend, subject, group1, group2
ORDER BY windowstart
```

</details>

<details>
<summary><code>sum_hour_group1_no_prewhere</code></summary>

```sql
SELECT
  tumbleStart(om_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(om_events.time, toIntervalHour(1), 'UTC') AS windowend,
  sum(toDecimal128OrNull(JSON_VALUE(om_events.data, '$.value'), 19)) AS value,
  om_events.subject AS subject,
  JSON_VALUE(om_events.data, '$.group1') AS group1
FROM om_events
WHERE om_events.namespace = {namespace:String}
  AND om_events.type = {type:String}
  AND om_events.subject IN {subjects:Array(String)}
  AND om_events.time >= {from:DateTime}
  AND om_events.time < {to:DateTime}
  AND JSON_VALUE(om_events.data, '$.group1') = {group1:String}
GROUP BY windowstart, windowend, subject, group1
ORDER BY windowstart
```

</details>

<details>
<summary><code>sum_hour_tz</code></summary>

```sql
SELECT
  tumbleStart(om_events.time, toIntervalHour(1), 'America/New_York') AS windowstart,
  tumbleEnd(om_events.time, toIntervalHour(1), 'America/New_York') AS windowend,
  sum(toDecimal128OrNull(JSON_VALUE(om_events.data, '$.value'), 19)) AS value,
  om_events.subject AS subject
FROM om_events
WHERE om_events.namespace = {namespace:String}
  AND om_events.type = {type:String}
  AND om_events.subject IN {subjects:Array(String)}
  AND om_events.time >= {from:DateTime}
  AND om_events.time < {to:DateTime}
GROUP BY windowstart, windowend, subject
ORDER BY windowstart
```

</details>

<details>
<summary><code>sum_minute</code></summary>

```sql
SELECT
  tumbleStart(om_events.time, toIntervalMinute(1), 'UTC') AS windowstart,
  tumbleEnd(om_events.time, toIntervalMinute(1), 'UTC') AS windowend,
  sum(toDecimal128OrNull(JSON_VALUE(om_events.data, '$.value'), 19)) AS value,
  om_events.subject AS subject
FROM om_events
WHERE om_events.namespace = {namespace:String}
  AND om_events.type = {type:String}
  AND om_events.subject IN {subjects:Array(String)}
  AND om_events.time >= {from:DateTime}
  AND om_events.time < {to:DateTime}
GROUP BY windowstart, windowend, subject
ORDER BY windowstart
```

</details>

<details>
<summary><code>sum_month</code></summary>

```sql
SELECT
  toDateTime(tumbleStart(om_events.time, toIntervalMonth(1), 'UTC'), 'UTC') AS windowstart,
  toDateTime(tumbleEnd(om_events.time, toIntervalMonth(1), 'UTC'), 'UTC') AS windowend,
  sum(toDecimal128OrNull(JSON_VALUE(om_events.data, '$.value'), 19)) AS value,
  om_events.subject AS subject
FROM om_events
WHERE om_events.namespace = {namespace:String}
  AND om_events.type = {type:String}
  AND om_events.subject IN {subjects:Array(String)}
  AND om_events.time >= {from:DateTime}
  AND om_events.time < {to:DateTime}
GROUP BY windowstart, windowend, subject
ORDER BY windowstart
```

</details>

<details>
<summary><code>sum_no_window</code></summary>

```sql
SELECT
  tumbleStart(min(om_events.time), toIntervalMinute(1), 'UTC') AS windowstart,
  tumbleEnd(max(om_events.time), toIntervalMinute(1), 'UTC') AS windowend,
  sum(toDecimal128OrNull(JSON_VALUE(om_events.data, '$.value'), 19)) AS value,
  om_events.subject AS subject
FROM om_events
WHERE om_events.namespace = {namespace:String}
  AND om_events.type = {type:String}
  AND om_events.subject IN {subjects:Array(String)}
  AND om_events.time >= {from:DateTime}
  AND om_events.time < {to:DateTime}
GROUP BY subject
```

</details>

<details>
<summary><code>unique_count_hour</code></summary>

```sql
SELECT
  tumbleStart(om_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(om_events.time, toIntervalHour(1), 'UTC') AS windowend,
  uniqExact(nullIf(JSON_VALUE(om_events.data, '$.value'), 'null')) AS value,
  om_events.subject AS subject
FROM om_events
WHERE om_events.namespace = {namespace:String}
  AND om_events.type = {type:String}
  AND om_events.subject IN {subjects:Array(String)}
  AND om_events.time >= {from:DateTime}
  AND om_events.time < {to:DateTime}
GROUP BY windowstart, windowend, subject
ORDER BY windowstart
```

</details>

<details>
<summary><code>workload_seconds_by_region</code></summary>

```sql
SELECT
  tumbleStart(om_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(om_events.time, toIntervalHour(1), 'UTC') AS windowend,
  sum(toDecimal128OrNull(JSON_VALUE(om_events.data, '$.duration_seconds'), 19)) AS value,
  JSON_VALUE(om_events.data, '$.region') AS region
FROM om_events
WHERE om_events.namespace = {namespace:String}
  AND om_events.type = 'workload'
  AND om_events.subject IN {subjects:Array(String)}
  AND om_events.time >= {from:DateTime}
  AND om_events.time < {to:DateTime}
GROUP BY windowstart, windowend, region
ORDER BY windowstart
```

</details>
