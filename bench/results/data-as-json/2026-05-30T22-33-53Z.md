# Benchmark: data-as-json

| Field | Value |
| --- | --- |
| Harness commit | `1270fa3-dirty` |
| ClickHouse | 26.2.5.45 (single-node) |
| Clusters | — |
| Sweep id | `20260530T223154Z` |
| Concurrency | 1 |
| Started | 2026-05-30T22:33:53Z |
| Finished | 2026-05-30T22:36:01Z |

## Ingest

Source `go-seeder`: 10000000 rows in 114.57s (87280 events/sec), batch=10000.

## Queries

| Query | c | cache | p50 ms | p95 ms | p99 ms | QPS | CPU p50 ms | CPU src | Mem MB |
| --- | ---: | --- | ---: | ---: | ---: | ---: | ---: | --- | ---: |
| agent_runs_by_name | 1 | warm | 7.0 | 19.0 | 19.0 | 50.5 | 58.1 | real_time | 1.4 |
| avg_hour | 1 | warm | 49.0 | 56.0 | 56.0 | 16.6 | 807.5 | real_time | 11.1 |
| count_hour | 1 | warm | 13.0 | 36.0 | 36.0 | 36.9 | 181.6 | real_time | 16.6 |
| kong_status_by_route | 1 | warm | 14.0 | 40.0 | 40.0 | 35.1 | 190.1 | real_time | 15.2 |
| latest_hour | 1 | warm | 49.0 | 84.0 | 84.0 | 15.8 | 800.1 | real_time | 12.3 |
| llm_tokens_by_model | 1 | warm | 15.0 | 32.0 | 32.0 | 36.3 | 201.7 | real_time | 12.6 |
| max_hour | 1 | warm | 46.0 | 84.0 | 84.0 | 16.3 | 765.5 | real_time | 12.2 |
| min_hour | 1 | warm | 46.0 | 84.0 | 84.0 | 16.2 | 769.2 | real_time | 12.2 |
| sum_day | 1 | warm | 47.0 | 82.0 | 82.0 | 16.2 | 770.4 | real_time | 8.2 |
| sum_hour | 1 | warm | 46.0 | 87.0 | 87.0 | 16.3 | 775.3 | real_time | 12.2 |
| sum_hour_group1 | 1 | warm | 31.0 | 68.0 | 68.0 | 21.6 | 481.4 | real_time | 5.0 |
| sum_hour_group1_group2 | 1 | warm | 27.0 | 49.0 | 49.0 | 25.1 | 396.2 | real_time | 5.1 |
| sum_hour_group1_group2_no_prewhere | 1 | warm | 25.0 | 60.0 | 60.0 | 24.7 | 377.9 | real_time | 4.6 |
| sum_hour_group1_no_prewhere | 1 | warm | 30.0 | 68.0 | 68.0 | 21.7 | 477.1 | real_time | 4.9 |
| sum_hour_tz | 1 | warm | 47.0 | 87.0 | 87.0 | 16.2 | 778.9 | real_time | 12.2 |
| sum_minute | 1 | warm | 58.0 | 102.0 | 102.0 | 13.3 | 948.9 | real_time | 40.6 |
| sum_month | 1 | warm | 47.0 | 86.0 | 86.0 | 16.1 | 778.2 | real_time | 8.2 |
| sum_no_window | 1 | warm | 44.0 | 56.0 | 56.0 | 17.7 | 730.1 | real_time | 8.2 |
| unique_count_hour | 1 | warm | 41.0 | 66.0 | 66.0 | 18.2 | 679.0 | real_time | 61.1 |
| workload_seconds_by_region | 1 | warm | 10.0 | 35.0 | 35.0 | 44.1 | 105.8 | real_time | 8.5 |

<details>
<summary><code>agent_runs_by_name</code></summary>

```sql
SELECT
  tumbleStart(data_as_json_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(data_as_json_events.time, toIntervalHour(1), 'UTC') AS windowend,
  count(*) AS value,
  toString(data_as_json_events.data.agent_name.:String) AS agent_name
FROM data_as_json_events
WHERE data_as_json_events.namespace = {namespace:String}
  AND data_as_json_events.type = 'agent_run'
  AND data_as_json_events.subject IN {subjects:Array(String)}
  AND data_as_json_events.time >= {from:DateTime}
  AND data_as_json_events.time < {to:DateTime}
GROUP BY windowstart, windowend, agent_name
ORDER BY windowstart
```

</details>

<details>
<summary><code>avg_hour</code></summary>

```sql
SELECT
  tumbleStart(data_as_json_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(data_as_json_events.time, toIntervalHour(1), 'UTC') AS windowend,
  avg(toDecimal128OrNull(toString(data_as_json_events.data.value), 19)) AS value,
  data_as_json_events.subject AS subject
FROM data_as_json_events
WHERE data_as_json_events.namespace = {namespace:String}
  AND data_as_json_events.type = {type:String}
  AND data_as_json_events.subject IN {subjects:Array(String)}
  AND data_as_json_events.time >= {from:DateTime}
  AND data_as_json_events.time < {to:DateTime}
GROUP BY windowstart, windowend, subject
ORDER BY windowstart
```

</details>

<details>
<summary><code>count_hour</code></summary>

```sql
SELECT
  tumbleStart(data_as_json_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(data_as_json_events.time, toIntervalHour(1), 'UTC') AS windowend,
  count(*) AS value,
  data_as_json_events.subject AS subject
FROM data_as_json_events
WHERE data_as_json_events.namespace = {namespace:String}
  AND data_as_json_events.type = {type:String}
  AND data_as_json_events.subject IN {subjects:Array(String)}
  AND data_as_json_events.time >= {from:DateTime}
  AND data_as_json_events.time < {to:DateTime}
GROUP BY windowstart, windowend, subject
ORDER BY windowstart
```

</details>

<details>
<summary><code>kong_status_by_route</code></summary>

```sql
SELECT
  tumbleStart(data_as_json_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(data_as_json_events.time, toIntervalHour(1), 'UTC') AS windowend,
  count(*) AS value,
  toString(data_as_json_events.data.response_http_status.:String) AS status,
  toString(data_as_json_events.data.route_name.:String) AS route
FROM data_as_json_events
WHERE data_as_json_events.namespace = {namespace:String}
  AND data_as_json_events.type = 'kong_api_request'
  AND data_as_json_events.subject IN {subjects:Array(String)}
  AND data_as_json_events.time >= {from:DateTime}
  AND data_as_json_events.time < {to:DateTime}
GROUP BY windowstart, windowend, status, route
ORDER BY windowstart
```

</details>

<details>
<summary><code>latest_hour</code></summary>

```sql
SELECT
  tumbleStart(data_as_json_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(data_as_json_events.time, toIntervalHour(1), 'UTC') AS windowend,
  argMax(toDecimal128OrNull(toString(data_as_json_events.data.value), 19), data_as_json_events.time) AS value,
  data_as_json_events.subject AS subject
FROM data_as_json_events
WHERE data_as_json_events.namespace = {namespace:String}
  AND data_as_json_events.type = {type:String}
  AND data_as_json_events.subject IN {subjects:Array(String)}
  AND data_as_json_events.time >= {from:DateTime}
  AND data_as_json_events.time < {to:DateTime}
GROUP BY windowstart, windowend, subject
ORDER BY windowstart
```

</details>

<details>
<summary><code>llm_tokens_by_model</code></summary>

```sql
SELECT
  tumbleStart(data_as_json_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(data_as_json_events.time, toIntervalHour(1), 'UTC') AS windowend,
  sum(toDecimal128OrNull(toString(data_as_json_events.data.tokens), 19)) AS value,
  toString(data_as_json_events.data.model.:String) AS model,
  toString(data_as_json_events.data.provider.:String) AS provider
FROM data_as_json_events
WHERE data_as_json_events.namespace = {namespace:String}
  AND data_as_json_events.type = 'llm_request'
  AND data_as_json_events.subject IN {subjects:Array(String)}
  AND data_as_json_events.time >= {from:DateTime}
  AND data_as_json_events.time < {to:DateTime}
GROUP BY windowstart, windowend, model, provider
ORDER BY windowstart
```

</details>

<details>
<summary><code>max_hour</code></summary>

```sql
SELECT
  tumbleStart(data_as_json_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(data_as_json_events.time, toIntervalHour(1), 'UTC') AS windowend,
  max(toDecimal128OrNull(toString(data_as_json_events.data.value), 19)) AS value,
  data_as_json_events.subject AS subject
FROM data_as_json_events
WHERE data_as_json_events.namespace = {namespace:String}
  AND data_as_json_events.type = {type:String}
  AND data_as_json_events.subject IN {subjects:Array(String)}
  AND data_as_json_events.time >= {from:DateTime}
  AND data_as_json_events.time < {to:DateTime}
GROUP BY windowstart, windowend, subject
ORDER BY windowstart
```

</details>

<details>
<summary><code>min_hour</code></summary>

```sql
SELECT
  tumbleStart(data_as_json_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(data_as_json_events.time, toIntervalHour(1), 'UTC') AS windowend,
  min(toDecimal128OrNull(toString(data_as_json_events.data.value), 19)) AS value,
  data_as_json_events.subject AS subject
FROM data_as_json_events
WHERE data_as_json_events.namespace = {namespace:String}
  AND data_as_json_events.type = {type:String}
  AND data_as_json_events.subject IN {subjects:Array(String)}
  AND data_as_json_events.time >= {from:DateTime}
  AND data_as_json_events.time < {to:DateTime}
GROUP BY windowstart, windowend, subject
ORDER BY windowstart
```

</details>

<details>
<summary><code>sum_day</code></summary>

```sql
SELECT
  tumbleStart(data_as_json_events.time, toIntervalDay(1), 'UTC') AS windowstart,
  tumbleEnd(data_as_json_events.time, toIntervalDay(1), 'UTC') AS windowend,
  sum(toDecimal128OrNull(toString(data_as_json_events.data.value), 19)) AS value,
  data_as_json_events.subject AS subject
FROM data_as_json_events
WHERE data_as_json_events.namespace = {namespace:String}
  AND data_as_json_events.type = {type:String}
  AND data_as_json_events.subject IN {subjects:Array(String)}
  AND data_as_json_events.time >= {from:DateTime}
  AND data_as_json_events.time < {to:DateTime}
GROUP BY windowstart, windowend, subject
ORDER BY windowstart
```

</details>

<details>
<summary><code>sum_hour</code></summary>

```sql
SELECT
  tumbleStart(data_as_json_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(data_as_json_events.time, toIntervalHour(1), 'UTC') AS windowend,
  sum(toDecimal128OrNull(toString(data_as_json_events.data.value), 19)) AS value,
  data_as_json_events.subject AS subject
FROM data_as_json_events
WHERE data_as_json_events.namespace = {namespace:String}
  AND data_as_json_events.type = {type:String}
  AND data_as_json_events.subject IN {subjects:Array(String)}
  AND data_as_json_events.time >= {from:DateTime}
  AND data_as_json_events.time < {to:DateTime}
GROUP BY windowstart, windowend, subject
ORDER BY windowstart
```

</details>

<details>
<summary><code>sum_hour_group1</code></summary>

```sql
SELECT
  tumbleStart(data_as_json_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(data_as_json_events.time, toIntervalHour(1), 'UTC') AS windowend,
  sum(toDecimal128OrNull(toString(data_as_json_events.data.value), 19)) AS value,
  data_as_json_events.subject AS subject,
  toString(data_as_json_events.data.group1.:String) AS group1
FROM data_as_json_events
WHERE data_as_json_events.namespace = {namespace:String}
  AND data_as_json_events.type = {type:String}
  AND data_as_json_events.subject IN {subjects:Array(String)}
  AND data_as_json_events.time >= {from:DateTime}
  AND data_as_json_events.time < {to:DateTime}
  AND toString(data_as_json_events.data.group1.:String) = {group1:String}
GROUP BY windowstart, windowend, subject, group1
ORDER BY windowstart
SETTINGS optimize_move_to_prewhere = 1, allow_reorder_prewhere_conditions = 1
```

</details>

<details>
<summary><code>sum_hour_group1_group2</code></summary>

```sql
SELECT
  tumbleStart(data_as_json_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(data_as_json_events.time, toIntervalHour(1), 'UTC') AS windowend,
  sum(toDecimal128OrNull(toString(data_as_json_events.data.value), 19)) AS value,
  data_as_json_events.subject AS subject,
  toString(data_as_json_events.data.group1.:String) AS group1,
  toString(data_as_json_events.data.group2.:String) AS group2
FROM data_as_json_events
WHERE data_as_json_events.namespace = {namespace:String}
  AND data_as_json_events.type = {type:String}
  AND data_as_json_events.subject IN {subjects:Array(String)}
  AND data_as_json_events.time >= {from:DateTime}
  AND data_as_json_events.time < {to:DateTime}
  AND toString(data_as_json_events.data.group1.:String) = {group1:String}
  AND toString(data_as_json_events.data.group2.:String) = {group2:String}
GROUP BY windowstart, windowend, subject, group1, group2
ORDER BY windowstart
SETTINGS optimize_move_to_prewhere = 1, allow_reorder_prewhere_conditions = 1
```

</details>

<details>
<summary><code>sum_hour_group1_group2_no_prewhere</code></summary>

```sql
SELECT
  tumbleStart(data_as_json_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(data_as_json_events.time, toIntervalHour(1), 'UTC') AS windowend,
  sum(toDecimal128OrNull(toString(data_as_json_events.data.value), 19)) AS value,
  data_as_json_events.subject AS subject,
  toString(data_as_json_events.data.group1.:String) AS group1,
  toString(data_as_json_events.data.group2.:String) AS group2
FROM data_as_json_events
WHERE data_as_json_events.namespace = {namespace:String}
  AND data_as_json_events.type = {type:String}
  AND data_as_json_events.subject IN {subjects:Array(String)}
  AND data_as_json_events.time >= {from:DateTime}
  AND data_as_json_events.time < {to:DateTime}
  AND toString(data_as_json_events.data.group1.:String) = {group1:String}
  AND toString(data_as_json_events.data.group2.:String) = {group2:String}
GROUP BY windowstart, windowend, subject, group1, group2
ORDER BY windowstart
```

</details>

<details>
<summary><code>sum_hour_group1_no_prewhere</code></summary>

```sql
SELECT
  tumbleStart(data_as_json_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(data_as_json_events.time, toIntervalHour(1), 'UTC') AS windowend,
  sum(toDecimal128OrNull(toString(data_as_json_events.data.value), 19)) AS value,
  data_as_json_events.subject AS subject,
  toString(data_as_json_events.data.group1.:String) AS group1
FROM data_as_json_events
WHERE data_as_json_events.namespace = {namespace:String}
  AND data_as_json_events.type = {type:String}
  AND data_as_json_events.subject IN {subjects:Array(String)}
  AND data_as_json_events.time >= {from:DateTime}
  AND data_as_json_events.time < {to:DateTime}
  AND toString(data_as_json_events.data.group1.:String) = {group1:String}
GROUP BY windowstart, windowend, subject, group1
ORDER BY windowstart
```

</details>

<details>
<summary><code>sum_hour_tz</code></summary>

```sql
SELECT
  tumbleStart(data_as_json_events.time, toIntervalHour(1), 'America/New_York') AS windowstart,
  tumbleEnd(data_as_json_events.time, toIntervalHour(1), 'America/New_York') AS windowend,
  sum(toDecimal128OrNull(toString(data_as_json_events.data.value), 19)) AS value,
  data_as_json_events.subject AS subject
FROM data_as_json_events
WHERE data_as_json_events.namespace = {namespace:String}
  AND data_as_json_events.type = {type:String}
  AND data_as_json_events.subject IN {subjects:Array(String)}
  AND data_as_json_events.time >= {from:DateTime}
  AND data_as_json_events.time < {to:DateTime}
GROUP BY windowstart, windowend, subject
ORDER BY windowstart
```

</details>

<details>
<summary><code>sum_minute</code></summary>

```sql
SELECT
  tumbleStart(data_as_json_events.time, toIntervalMinute(1), 'UTC') AS windowstart,
  tumbleEnd(data_as_json_events.time, toIntervalMinute(1), 'UTC') AS windowend,
  sum(toDecimal128OrNull(toString(data_as_json_events.data.value), 19)) AS value,
  data_as_json_events.subject AS subject
FROM data_as_json_events
WHERE data_as_json_events.namespace = {namespace:String}
  AND data_as_json_events.type = {type:String}
  AND data_as_json_events.subject IN {subjects:Array(String)}
  AND data_as_json_events.time >= {from:DateTime}
  AND data_as_json_events.time < {to:DateTime}
GROUP BY windowstart, windowend, subject
ORDER BY windowstart
```

</details>

<details>
<summary><code>sum_month</code></summary>

```sql
SELECT
  toDateTime(tumbleStart(data_as_json_events.time, toIntervalMonth(1), 'UTC'), 'UTC') AS windowstart,
  toDateTime(tumbleEnd(data_as_json_events.time, toIntervalMonth(1), 'UTC'), 'UTC') AS windowend,
  sum(toDecimal128OrNull(toString(data_as_json_events.data.value), 19)) AS value,
  data_as_json_events.subject AS subject
FROM data_as_json_events
WHERE data_as_json_events.namespace = {namespace:String}
  AND data_as_json_events.type = {type:String}
  AND data_as_json_events.subject IN {subjects:Array(String)}
  AND data_as_json_events.time >= {from:DateTime}
  AND data_as_json_events.time < {to:DateTime}
GROUP BY windowstart, windowend, subject
ORDER BY windowstart
```

</details>

<details>
<summary><code>sum_no_window</code></summary>

```sql
SELECT
  tumbleStart(min(data_as_json_events.time), toIntervalMinute(1), 'UTC') AS windowstart,
  tumbleEnd(max(data_as_json_events.time), toIntervalMinute(1), 'UTC') AS windowend,
  sum(toDecimal128OrNull(toString(data_as_json_events.data.value), 19)) AS value,
  data_as_json_events.subject AS subject
FROM data_as_json_events
WHERE data_as_json_events.namespace = {namespace:String}
  AND data_as_json_events.type = {type:String}
  AND data_as_json_events.subject IN {subjects:Array(String)}
  AND data_as_json_events.time >= {from:DateTime}
  AND data_as_json_events.time < {to:DateTime}
GROUP BY subject
```

</details>

<details>
<summary><code>unique_count_hour</code></summary>

```sql
SELECT
  tumbleStart(data_as_json_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(data_as_json_events.time, toIntervalHour(1), 'UTC') AS windowend,
  uniqExact(nullIf(toString(data_as_json_events.data.value), 'null')) AS value,
  data_as_json_events.subject AS subject
FROM data_as_json_events
WHERE data_as_json_events.namespace = {namespace:String}
  AND data_as_json_events.type = {type:String}
  AND data_as_json_events.subject IN {subjects:Array(String)}
  AND data_as_json_events.time >= {from:DateTime}
  AND data_as_json_events.time < {to:DateTime}
GROUP BY windowstart, windowend, subject
ORDER BY windowstart
```

</details>

<details>
<summary><code>workload_seconds_by_region</code></summary>

```sql
SELECT
  tumbleStart(data_as_json_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(data_as_json_events.time, toIntervalHour(1), 'UTC') AS windowend,
  sum(toDecimal128OrNull(toString(data_as_json_events.data.duration_seconds), 19)) AS value,
  toString(data_as_json_events.data.region.:String) AS region
FROM data_as_json_events
WHERE data_as_json_events.namespace = {namespace:String}
  AND data_as_json_events.type = 'workload'
  AND data_as_json_events.subject IN {subjects:Array(String)}
  AND data_as_json_events.time >= {from:DateTime}
  AND data_as_json_events.time < {to:DateTime}
GROUP BY windowstart, windowend, region
ORDER BY windowstart
```

</details>
