# Benchmark: data-as-json

| Field | Value |
| --- | --- |
| Harness commit | `858a46d-dirty` |
| ClickHouse | 26.2.19.43 (single-node) |
| Clusters | default |
| Sweep id | `20260530T150315Z` |
| Concurrency | 1 |
| Started | 2026-05-30T15:05:08Z |
| Finished | 2026-05-30T15:07:11Z |

## Ingest

Source `go-seeder`: 10000000 rows in 108.68s (92010 events/sec), batch=10000.

## Queries

| Query | p50 ms | p95 ms | p99 ms | QPS | CPU p50 ms | CPU src | Mem MB |
| --- | ---: | ---: | ---: | ---: | ---: | --- | ---: |
| agent_runs_by_name | 6.0 | 7.0 | 7.0 | 55.3 | 20.3 | os_cpu | 11.1 |
| avg_hour | 42.0 | 44.0 | 44.0 | 18.6 | 100.5 | os_cpu | 10.6 |
| count_hour | 11.0 | 12.0 | 12.0 | 44.2 | 24.3 | os_cpu | 7.2 |
| kong_status_by_route | 12.0 | 15.0 | 15.0 | 41.8 | 27.6 | os_cpu | 11.6 |
| latest_hour | 43.0 | 52.0 | 52.0 | 18.1 | 101.9 | os_cpu | 9.6 |
| llm_tokens_by_model | 12.0 | 15.0 | 15.0 | 41.0 | 28.4 | os_cpu | 15.3 |
| max_hour | 42.0 | 49.0 | 49.0 | 18.5 | 100.3 | os_cpu | 9.3 |
| min_hour | 41.0 | 43.0 | 43.0 | 18.6 | 100.2 | os_cpu | 9.3 |
| sum_day | 41.0 | 51.0 | 51.0 | 18.3 | 100.0 | os_cpu | 9.1 |
| sum_hour | 42.0 | 43.0 | 43.0 | 18.5 | 100.3 | os_cpu | 9.3 |
| sum_hour_group1 | 26.0 | 27.0 | 27.0 | 26.9 | 61.9 | os_cpu | 10.5 |
| sum_hour_group1_group2 | 22.0 | 31.0 | 31.0 | 29.4 | 51.7 | os_cpu | 14.5 |
| sum_hour_group1_group2_no_prewhere | 21.0 | 23.0 | 23.0 | 29.7 | 52.0 | os_cpu | 15.0 |
| sum_hour_group1_no_prewhere | 26.0 | 32.0 | 32.0 | 25.7 | 63.3 | os_cpu | 10.9 |
| sum_hour_tz | 41.0 | 53.0 | 53.0 | 18.5 | 99.0 | os_cpu | 9.3 |
| sum_minute | 54.0 | 59.0 | 59.0 | 14.8 | 143.8 | os_cpu | 37.6 |
| sum_month | 44.0 | 52.0 | 52.0 | 17.8 | 105.4 | os_cpu | 9.1 |
| sum_no_window | 40.0 | 41.0 | 41.0 | 19.4 | 95.4 | os_cpu | 8.9 |
| unique_count_hour | 39.0 | 116.0 | 116.0 | 17.2 | 104.3 | os_cpu | 60.8 |
| workload_seconds_by_region | 15.0 | 32.0 | 32.0 | 23.1 | 34.0 | os_cpu | 7.0 |

<details>
<summary><code>agent_runs_by_name</code></summary>

```sql
SELECT
  tumbleStart(om_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(om_events.time, toIntervalHour(1), 'UTC') AS windowend,
  count(*) AS value,
  toString(om_events.data.agent_name.:String) AS agent_name
FROM om_events
WHERE om_events.namespace = {namespace:String}
  AND om_events.type = 'agent_run'
  AND om_events.subject IN {subjects:Array(String)}
  AND om_events.time >= {from:DateTime}
  AND om_events.time < {to:DateTime}
GROUP BY windowstart, windowend, agent_name
ORDER BY windowstart
```

</details>

<details>
<summary><code>avg_hour</code></summary>

```sql
SELECT
  tumbleStart(om_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(om_events.time, toIntervalHour(1), 'UTC') AS windowend,
  avg(toDecimal128OrNull(toString(om_events.data.value), 19)) AS value,
  om_events.subject AS subject
FROM om_events
WHERE om_events.namespace = {namespace:String}
  AND om_events.type = {type:String}
  AND om_events.subject IN {subjects:Array(String)}
  AND om_events.time >= {from:DateTime}
  AND om_events.time < {to:DateTime}
GROUP BY windowstart, windowend, subject
ORDER BY windowstart
```

</details>

<details>
<summary><code>count_hour</code></summary>

```sql
SELECT
  tumbleStart(om_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(om_events.time, toIntervalHour(1), 'UTC') AS windowend,
  count(*) AS value,
  om_events.subject AS subject
FROM om_events
WHERE om_events.namespace = {namespace:String}
  AND om_events.type = {type:String}
  AND om_events.subject IN {subjects:Array(String)}
  AND om_events.time >= {from:DateTime}
  AND om_events.time < {to:DateTime}
GROUP BY windowstart, windowend, subject
ORDER BY windowstart
```

</details>

<details>
<summary><code>kong_status_by_route</code></summary>

```sql
SELECT
  tumbleStart(om_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(om_events.time, toIntervalHour(1), 'UTC') AS windowend,
  count(*) AS value,
  toString(om_events.data.response_http_status.:String) AS status,
  toString(om_events.data.route_name.:String) AS route
FROM om_events
WHERE om_events.namespace = {namespace:String}
  AND om_events.type = 'kong_api_request'
  AND om_events.subject IN {subjects:Array(String)}
  AND om_events.time >= {from:DateTime}
  AND om_events.time < {to:DateTime}
GROUP BY windowstart, windowend, status, route
ORDER BY windowstart
```

</details>

<details>
<summary><code>latest_hour</code></summary>

```sql
SELECT
  tumbleStart(om_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(om_events.time, toIntervalHour(1), 'UTC') AS windowend,
  argMax(toDecimal128OrNull(toString(om_events.data.value), 19), om_events.time) AS value,
  om_events.subject AS subject
FROM om_events
WHERE om_events.namespace = {namespace:String}
  AND om_events.type = {type:String}
  AND om_events.subject IN {subjects:Array(String)}
  AND om_events.time >= {from:DateTime}
  AND om_events.time < {to:DateTime}
GROUP BY windowstart, windowend, subject
ORDER BY windowstart
```

</details>

<details>
<summary><code>llm_tokens_by_model</code></summary>

```sql
SELECT
  tumbleStart(om_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(om_events.time, toIntervalHour(1), 'UTC') AS windowend,
  sum(toDecimal128OrNull(toString(om_events.data.tokens), 19)) AS value,
  toString(om_events.data.model.:String) AS model,
  toString(om_events.data.provider.:String) AS provider
FROM om_events
WHERE om_events.namespace = {namespace:String}
  AND om_events.type = 'llm_request'
  AND om_events.subject IN {subjects:Array(String)}
  AND om_events.time >= {from:DateTime}
  AND om_events.time < {to:DateTime}
GROUP BY windowstart, windowend, model, provider
ORDER BY windowstart
```

</details>

<details>
<summary><code>max_hour</code></summary>

```sql
SELECT
  tumbleStart(om_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(om_events.time, toIntervalHour(1), 'UTC') AS windowend,
  max(toDecimal128OrNull(toString(om_events.data.value), 19)) AS value,
  om_events.subject AS subject
FROM om_events
WHERE om_events.namespace = {namespace:String}
  AND om_events.type = {type:String}
  AND om_events.subject IN {subjects:Array(String)}
  AND om_events.time >= {from:DateTime}
  AND om_events.time < {to:DateTime}
GROUP BY windowstart, windowend, subject
ORDER BY windowstart
```

</details>

<details>
<summary><code>min_hour</code></summary>

```sql
SELECT
  tumbleStart(om_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(om_events.time, toIntervalHour(1), 'UTC') AS windowend,
  min(toDecimal128OrNull(toString(om_events.data.value), 19)) AS value,
  om_events.subject AS subject
FROM om_events
WHERE om_events.namespace = {namespace:String}
  AND om_events.type = {type:String}
  AND om_events.subject IN {subjects:Array(String)}
  AND om_events.time >= {from:DateTime}
  AND om_events.time < {to:DateTime}
GROUP BY windowstart, windowend, subject
ORDER BY windowstart
```

</details>

<details>
<summary><code>sum_day</code></summary>

```sql
SELECT
  tumbleStart(om_events.time, toIntervalDay(1), 'UTC') AS windowstart,
  tumbleEnd(om_events.time, toIntervalDay(1), 'UTC') AS windowend,
  sum(toDecimal128OrNull(toString(om_events.data.value), 19)) AS value,
  om_events.subject AS subject
FROM om_events
WHERE om_events.namespace = {namespace:String}
  AND om_events.type = {type:String}
  AND om_events.subject IN {subjects:Array(String)}
  AND om_events.time >= {from:DateTime}
  AND om_events.time < {to:DateTime}
GROUP BY windowstart, windowend, subject
ORDER BY windowstart
```

</details>

<details>
<summary><code>sum_hour</code></summary>

```sql
SELECT
  tumbleStart(om_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(om_events.time, toIntervalHour(1), 'UTC') AS windowend,
  sum(toDecimal128OrNull(toString(om_events.data.value), 19)) AS value,
  om_events.subject AS subject
FROM om_events
WHERE om_events.namespace = {namespace:String}
  AND om_events.type = {type:String}
  AND om_events.subject IN {subjects:Array(String)}
  AND om_events.time >= {from:DateTime}
  AND om_events.time < {to:DateTime}
GROUP BY windowstart, windowend, subject
ORDER BY windowstart
```

</details>

<details>
<summary><code>sum_hour_group1</code></summary>

```sql
SELECT
  tumbleStart(om_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(om_events.time, toIntervalHour(1), 'UTC') AS windowend,
  sum(toDecimal128OrNull(toString(om_events.data.value), 19)) AS value,
  om_events.subject AS subject,
  toString(om_events.data.group1.:String) AS group1
FROM om_events
WHERE om_events.namespace = {namespace:String}
  AND om_events.type = {type:String}
  AND om_events.subject IN {subjects:Array(String)}
  AND om_events.time >= {from:DateTime}
  AND om_events.time < {to:DateTime}
  AND toString(om_events.data.group1.:String) = {group1:String}
GROUP BY windowstart, windowend, subject, group1
ORDER BY windowstart
SETTINGS optimize_move_to_prewhere = 1, allow_reorder_prewhere_conditions = 1
```

</details>

<details>
<summary><code>sum_hour_group1_group2</code></summary>

```sql
SELECT
  tumbleStart(om_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(om_events.time, toIntervalHour(1), 'UTC') AS windowend,
  sum(toDecimal128OrNull(toString(om_events.data.value), 19)) AS value,
  om_events.subject AS subject,
  toString(om_events.data.group1.:String) AS group1,
  toString(om_events.data.group2.:String) AS group2
FROM om_events
WHERE om_events.namespace = {namespace:String}
  AND om_events.type = {type:String}
  AND om_events.subject IN {subjects:Array(String)}
  AND om_events.time >= {from:DateTime}
  AND om_events.time < {to:DateTime}
  AND toString(om_events.data.group1.:String) = {group1:String}
  AND toString(om_events.data.group2.:String) = {group2:String}
GROUP BY windowstart, windowend, subject, group1, group2
ORDER BY windowstart
SETTINGS optimize_move_to_prewhere = 1, allow_reorder_prewhere_conditions = 1
```

</details>

<details>
<summary><code>sum_hour_group1_group2_no_prewhere</code></summary>

```sql
SELECT
  tumbleStart(om_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(om_events.time, toIntervalHour(1), 'UTC') AS windowend,
  sum(toDecimal128OrNull(toString(om_events.data.value), 19)) AS value,
  om_events.subject AS subject,
  toString(om_events.data.group1.:String) AS group1,
  toString(om_events.data.group2.:String) AS group2
FROM om_events
WHERE om_events.namespace = {namespace:String}
  AND om_events.type = {type:String}
  AND om_events.subject IN {subjects:Array(String)}
  AND om_events.time >= {from:DateTime}
  AND om_events.time < {to:DateTime}
  AND toString(om_events.data.group1.:String) = {group1:String}
  AND toString(om_events.data.group2.:String) = {group2:String}
GROUP BY windowstart, windowend, subject, group1, group2
ORDER BY windowstart
```

</details>

<details>
<summary><code>sum_hour_group1_no_prewhere</code></summary>

```sql
SELECT
  tumbleStart(om_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(om_events.time, toIntervalHour(1), 'UTC') AS windowend,
  sum(toDecimal128OrNull(toString(om_events.data.value), 19)) AS value,
  om_events.subject AS subject,
  toString(om_events.data.group1.:String) AS group1
FROM om_events
WHERE om_events.namespace = {namespace:String}
  AND om_events.type = {type:String}
  AND om_events.subject IN {subjects:Array(String)}
  AND om_events.time >= {from:DateTime}
  AND om_events.time < {to:DateTime}
  AND toString(om_events.data.group1.:String) = {group1:String}
GROUP BY windowstart, windowend, subject, group1
ORDER BY windowstart
```

</details>

<details>
<summary><code>sum_hour_tz</code></summary>

```sql
SELECT
  tumbleStart(om_events.time, toIntervalHour(1), 'America/New_York') AS windowstart,
  tumbleEnd(om_events.time, toIntervalHour(1), 'America/New_York') AS windowend,
  sum(toDecimal128OrNull(toString(om_events.data.value), 19)) AS value,
  om_events.subject AS subject
FROM om_events
WHERE om_events.namespace = {namespace:String}
  AND om_events.type = {type:String}
  AND om_events.subject IN {subjects:Array(String)}
  AND om_events.time >= {from:DateTime}
  AND om_events.time < {to:DateTime}
GROUP BY windowstart, windowend, subject
ORDER BY windowstart
```

</details>

<details>
<summary><code>sum_minute</code></summary>

```sql
SELECT
  tumbleStart(om_events.time, toIntervalMinute(1), 'UTC') AS windowstart,
  tumbleEnd(om_events.time, toIntervalMinute(1), 'UTC') AS windowend,
  sum(toDecimal128OrNull(toString(om_events.data.value), 19)) AS value,
  om_events.subject AS subject
FROM om_events
WHERE om_events.namespace = {namespace:String}
  AND om_events.type = {type:String}
  AND om_events.subject IN {subjects:Array(String)}
  AND om_events.time >= {from:DateTime}
  AND om_events.time < {to:DateTime}
GROUP BY windowstart, windowend, subject
ORDER BY windowstart
```

</details>

<details>
<summary><code>sum_month</code></summary>

```sql
SELECT
  toDateTime(tumbleStart(om_events.time, toIntervalMonth(1), 'UTC'), 'UTC') AS windowstart,
  toDateTime(tumbleEnd(om_events.time, toIntervalMonth(1), 'UTC'), 'UTC') AS windowend,
  sum(toDecimal128OrNull(toString(om_events.data.value), 19)) AS value,
  om_events.subject AS subject
FROM om_events
WHERE om_events.namespace = {namespace:String}
  AND om_events.type = {type:String}
  AND om_events.subject IN {subjects:Array(String)}
  AND om_events.time >= {from:DateTime}
  AND om_events.time < {to:DateTime}
GROUP BY windowstart, windowend, subject
ORDER BY windowstart
```

</details>

<details>
<summary><code>sum_no_window</code></summary>

```sql
SELECT
  tumbleStart(min(om_events.time), toIntervalMinute(1), 'UTC') AS windowstart,
  tumbleEnd(max(om_events.time), toIntervalMinute(1), 'UTC') AS windowend,
  sum(toDecimal128OrNull(toString(om_events.data.value), 19)) AS value,
  om_events.subject AS subject
FROM om_events
WHERE om_events.namespace = {namespace:String}
  AND om_events.type = {type:String}
  AND om_events.subject IN {subjects:Array(String)}
  AND om_events.time >= {from:DateTime}
  AND om_events.time < {to:DateTime}
GROUP BY subject
```

</details>

<details>
<summary><code>unique_count_hour</code></summary>

```sql
SELECT
  tumbleStart(om_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(om_events.time, toIntervalHour(1), 'UTC') AS windowend,
  uniqExact(nullIf(toString(om_events.data.value.:Float64), 'null')) AS value,
  om_events.subject AS subject
FROM om_events
WHERE om_events.namespace = {namespace:String}
  AND om_events.type = {type:String}
  AND om_events.subject IN {subjects:Array(String)}
  AND om_events.time >= {from:DateTime}
  AND om_events.time < {to:DateTime}
GROUP BY windowstart, windowend, subject
ORDER BY windowstart
```

</details>

<details>
<summary><code>workload_seconds_by_region</code></summary>

```sql
SELECT
  tumbleStart(om_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(om_events.time, toIntervalHour(1), 'UTC') AS windowend,
  sum(toDecimal128OrNull(toString(om_events.data.duration_seconds), 19)) AS value,
  toString(om_events.data.region.:String) AS region
FROM om_events
WHERE om_events.namespace = {namespace:String}
  AND om_events.type = 'workload'
  AND om_events.subject IN {subjects:Array(String)}
  AND om_events.time >= {from:DateTime}
  AND om_events.time < {to:DateTime}
GROUP BY windowstart, windowend, region
ORDER BY windowstart
```

</details>
