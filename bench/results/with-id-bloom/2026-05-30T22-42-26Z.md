# Benchmark: with-id-bloom

| Field | Value |
| --- | --- |
| Harness commit | `1270fa3-dirty` |
| ClickHouse | 26.2.5.45 (single-node) |
| Clusters | — |
| Sweep id | `20260530T223154Z` |
| Concurrency | 1 |
| Started | 2026-05-30T22:42:26Z |
| Finished | 2026-05-30T22:44:36Z |

## Ingest

Source `go-seeder`: 10000000 rows in 128.82s (77626 events/sec), batch=10000.

## Queries

| Query | c | cache | p50 ms | p95 ms | p99 ms | QPS | CPU p50 ms | CPU src | Mem MB |
| --- | ---: | --- | ---: | ---: | ---: | ---: | ---: | --- | ---: |
| lookup_by_id | 1 | warm | 62.0 | 97.0 | 97.0 | 12.6 | 951.1 | real_time | 375.7 |

### Index pruning (`EXPLAIN indexes=1`, literal id)

| Query | granules (no index → index) | parts (no index → index) |
| --- | ---: | ---: |
| lookup_by_id | 1226 → 18 | 18 → 6 |

<details>
<summary><code>lookup_by_id</code></summary>

```sql
SELECT
  with_id_bloom_events.id AS id,
  with_id_bloom_events.type AS type,
  with_id_bloom_events.subject AS subject,
  with_id_bloom_events.source AS source,
  with_id_bloom_events.time AS time,
  with_id_bloom_events.data AS data,
  with_id_bloom_events.ingested_at AS ingested_at,
  with_id_bloom_events.stored_at AS stored_at,
  with_id_bloom_events.store_row_id AS store_row_id
FROM with_id_bloom_events
WHERE with_id_bloom_events.namespace = {namespace:String}
  AND with_id_bloom_events.id = (
    SELECT id FROM with_id_bloom_events
    WHERE namespace = {namespace:String}
    ORDER BY namespace, type, subject, time
    LIMIT 1 OFFSET 100000
  )
```

</details>
