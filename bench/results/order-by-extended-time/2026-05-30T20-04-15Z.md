# Benchmark: order-by-extended-time

| Field | Value |
| --- | --- |
| Harness commit | `3907e52-dirty` |
| ClickHouse | 26.2.19.43 (single-node) |
| Clusters | default |
| Sweep id | `20260530T195822Z` |
| Concurrency | 1 |
| Started | 2026-05-30T20:04:15Z |
| Finished | 2026-05-30T20:06:17Z |

## Ingest

Source `go-seeder`: 10000000 rows in 109.21s (91567 events/sec), batch=10000.

## Queries

| Query | p50 ms | p95 ms | p99 ms | QPS | CPU p50 ms | CPU src | Mem MB |
| --- | ---: | ---: | ---: | ---: | ---: | --- | ---: |
| agent_runs_by_name | 6.0 | 7.0 | 7.0 | 43.9 | 19.9 | os_cpu | 12.9 |
| avg_hour | 44.0 | 49.0 | 49.0 | 17.5 | 106.1 | os_cpu | 9.7 |
| count_hour | 11.0 | 12.0 | 12.0 | 44.2 | 24.4 | os_cpu | 7.1 |
| kong_status_by_route | 12.0 | 14.0 | 14.0 | 41.6 | 28.1 | os_cpu | 11.9 |
| latest_hour | 45.0 | 49.0 | 49.0 | 17.7 | 108.2 | os_cpu | 9.6 |
| llm_tokens_by_model | 13.0 | 15.0 | 15.0 | 40.3 | 29.6 | os_cpu | 15.4 |
| max_hour | 43.0 | 48.0 | 48.0 | 18.3 | 102.1 | os_cpu | 9.3 |
| min_hour | 42.0 | 45.0 | 45.0 | 18.2 | 101.5 | os_cpu | 9.3 |
| sum_day | 43.0 | 54.0 | 54.0 | 17.9 | 101.9 | os_cpu | 9.1 |
| sum_hour | 43.0 | 45.0 | 45.0 | 18.2 | 102.9 | os_cpu | 9.3 |
| sum_hour_group1 | 27.0 | 36.0 | 36.0 | 24.9 | 64.2 | os_cpu | 11.0 |
| sum_hour_group1_group2 | 23.0 | 30.0 | 30.0 | 27.9 | 54.1 | os_cpu | 15.2 |
| sum_hour_group1_group2_no_prewhere | 22.0 | 23.0 | 23.0 | 29.5 | 53.6 | os_cpu | 13.5 |
| sum_hour_group1_no_prewhere | 27.0 | 34.0 | 34.0 | 24.8 | 65.0 | os_cpu | 10.5 |
| sum_hour_tz | 43.0 | 50.0 | 50.0 | 17.9 | 103.2 | os_cpu | 9.3 |
| sum_minute | 55.0 | 63.0 | 63.0 | 14.4 | 146.0 | os_cpu | 37.7 |
| sum_month | 44.0 | 47.0 | 47.0 | 17.8 | 105.1 | os_cpu | 9.1 |
| sum_no_window | 40.0 | 42.0 | 42.0 | 19.2 | 95.6 | os_cpu | 8.9 |
| unique_count_hour | 34.0 | 38.0 | 38.0 | 21.3 | 98.8 | os_cpu | 61.0 |
| workload_seconds_by_region | 8.0 | 13.0 | 13.0 | 47.8 | 18.8 | os_cpu | 6.7 |

<details>
<summary><code>agent_runs_by_name</code></summary>

```sql
SELECT
  tumbleStart(order_by_extended_time_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(order_by_extended_time_events.time, toIntervalHour(1), 'UTC') AS windowend,
  count(*) AS value,
  toString(order_by_extended_time_events.data.agent_name.:String) AS agent_name
FROM order_by_extended_time_events
WHERE order_by_extended_time_events.namespace = {namespace:String}
  AND order_by_extended_time_events.type = 'agent_run'
  AND order_by_extended_time_events.subject IN {subjects:Array(String)}
  AND order_by_extended_time_events.time >= {from:DateTime}
  AND order_by_extended_time_events.time < {to:DateTime}
GROUP BY windowstart, windowend, agent_name
ORDER BY windowstart
```

</details>

<details>
<summary><code>avg_hour</code></summary>

```sql
SELECT
  tumbleStart(order_by_extended_time_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(order_by_extended_time_events.time, toIntervalHour(1), 'UTC') AS windowend,
  avg(toDecimal128OrNull(toString(order_by_extended_time_events.data.value), 19)) AS value,
  order_by_extended_time_events.subject AS subject
FROM order_by_extended_time_events
WHERE order_by_extended_time_events.namespace = {namespace:String}
  AND order_by_extended_time_events.type = {type:String}
  AND order_by_extended_time_events.subject IN {subjects:Array(String)}
  AND order_by_extended_time_events.time >= {from:DateTime}
  AND order_by_extended_time_events.time < {to:DateTime}
GROUP BY windowstart, windowend, subject
ORDER BY windowstart
```

</details>

<details>
<summary><code>count_hour</code></summary>

```sql
SELECT
  tumbleStart(order_by_extended_time_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(order_by_extended_time_events.time, toIntervalHour(1), 'UTC') AS windowend,
  count(*) AS value,
  order_by_extended_time_events.subject AS subject
FROM order_by_extended_time_events
WHERE order_by_extended_time_events.namespace = {namespace:String}
  AND order_by_extended_time_events.type = {type:String}
  AND order_by_extended_time_events.subject IN {subjects:Array(String)}
  AND order_by_extended_time_events.time >= {from:DateTime}
  AND order_by_extended_time_events.time < {to:DateTime}
GROUP BY windowstart, windowend, subject
ORDER BY windowstart
```

</details>

<details>
<summary><code>kong_status_by_route</code></summary>

```sql
SELECT
  tumbleStart(order_by_extended_time_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(order_by_extended_time_events.time, toIntervalHour(1), 'UTC') AS windowend,
  count(*) AS value,
  toString(order_by_extended_time_events.data.response_http_status.:String) AS status,
  toString(order_by_extended_time_events.data.route_name.:String) AS route
FROM order_by_extended_time_events
WHERE order_by_extended_time_events.namespace = {namespace:String}
  AND order_by_extended_time_events.type = 'kong_api_request'
  AND order_by_extended_time_events.subject IN {subjects:Array(String)}
  AND order_by_extended_time_events.time >= {from:DateTime}
  AND order_by_extended_time_events.time < {to:DateTime}
GROUP BY windowstart, windowend, status, route
ORDER BY windowstart
```

</details>

<details>
<summary><code>latest_hour</code></summary>

```sql
SELECT
  tumbleStart(order_by_extended_time_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(order_by_extended_time_events.time, toIntervalHour(1), 'UTC') AS windowend,
  argMax(toDecimal128OrNull(toString(order_by_extended_time_events.data.value), 19), order_by_extended_time_events.time) AS value,
  order_by_extended_time_events.subject AS subject
FROM order_by_extended_time_events
WHERE order_by_extended_time_events.namespace = {namespace:String}
  AND order_by_extended_time_events.type = {type:String}
  AND order_by_extended_time_events.subject IN {subjects:Array(String)}
  AND order_by_extended_time_events.time >= {from:DateTime}
  AND order_by_extended_time_events.time < {to:DateTime}
GROUP BY windowstart, windowend, subject
ORDER BY windowstart
```

</details>

<details>
<summary><code>llm_tokens_by_model</code></summary>

```sql
SELECT
  tumbleStart(order_by_extended_time_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(order_by_extended_time_events.time, toIntervalHour(1), 'UTC') AS windowend,
  sum(toDecimal128OrNull(toString(order_by_extended_time_events.data.tokens), 19)) AS value,
  toString(order_by_extended_time_events.data.model.:String) AS model,
  toString(order_by_extended_time_events.data.provider.:String) AS provider
FROM order_by_extended_time_events
WHERE order_by_extended_time_events.namespace = {namespace:String}
  AND order_by_extended_time_events.type = 'llm_request'
  AND order_by_extended_time_events.subject IN {subjects:Array(String)}
  AND order_by_extended_time_events.time >= {from:DateTime}
  AND order_by_extended_time_events.time < {to:DateTime}
GROUP BY windowstart, windowend, model, provider
ORDER BY windowstart
```

</details>

<details>
<summary><code>max_hour</code></summary>

```sql
SELECT
  tumbleStart(order_by_extended_time_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(order_by_extended_time_events.time, toIntervalHour(1), 'UTC') AS windowend,
  max(toDecimal128OrNull(toString(order_by_extended_time_events.data.value), 19)) AS value,
  order_by_extended_time_events.subject AS subject
FROM order_by_extended_time_events
WHERE order_by_extended_time_events.namespace = {namespace:String}
  AND order_by_extended_time_events.type = {type:String}
  AND order_by_extended_time_events.subject IN {subjects:Array(String)}
  AND order_by_extended_time_events.time >= {from:DateTime}
  AND order_by_extended_time_events.time < {to:DateTime}
GROUP BY windowstart, windowend, subject
ORDER BY windowstart
```

</details>

<details>
<summary><code>min_hour</code></summary>

```sql
SELECT
  tumbleStart(order_by_extended_time_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(order_by_extended_time_events.time, toIntervalHour(1), 'UTC') AS windowend,
  min(toDecimal128OrNull(toString(order_by_extended_time_events.data.value), 19)) AS value,
  order_by_extended_time_events.subject AS subject
FROM order_by_extended_time_events
WHERE order_by_extended_time_events.namespace = {namespace:String}
  AND order_by_extended_time_events.type = {type:String}
  AND order_by_extended_time_events.subject IN {subjects:Array(String)}
  AND order_by_extended_time_events.time >= {from:DateTime}
  AND order_by_extended_time_events.time < {to:DateTime}
GROUP BY windowstart, windowend, subject
ORDER BY windowstart
```

</details>

<details>
<summary><code>sum_day</code></summary>

```sql
SELECT
  tumbleStart(order_by_extended_time_events.time, toIntervalDay(1), 'UTC') AS windowstart,
  tumbleEnd(order_by_extended_time_events.time, toIntervalDay(1), 'UTC') AS windowend,
  sum(toDecimal128OrNull(toString(order_by_extended_time_events.data.value), 19)) AS value,
  order_by_extended_time_events.subject AS subject
FROM order_by_extended_time_events
WHERE order_by_extended_time_events.namespace = {namespace:String}
  AND order_by_extended_time_events.type = {type:String}
  AND order_by_extended_time_events.subject IN {subjects:Array(String)}
  AND order_by_extended_time_events.time >= {from:DateTime}
  AND order_by_extended_time_events.time < {to:DateTime}
GROUP BY windowstart, windowend, subject
ORDER BY windowstart
```

</details>

<details>
<summary><code>sum_hour</code></summary>

```sql
SELECT
  tumbleStart(order_by_extended_time_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(order_by_extended_time_events.time, toIntervalHour(1), 'UTC') AS windowend,
  sum(toDecimal128OrNull(toString(order_by_extended_time_events.data.value), 19)) AS value,
  order_by_extended_time_events.subject AS subject
FROM order_by_extended_time_events
WHERE order_by_extended_time_events.namespace = {namespace:String}
  AND order_by_extended_time_events.type = {type:String}
  AND order_by_extended_time_events.subject IN {subjects:Array(String)}
  AND order_by_extended_time_events.time >= {from:DateTime}
  AND order_by_extended_time_events.time < {to:DateTime}
GROUP BY windowstart, windowend, subject
ORDER BY windowstart
```

</details>

<details>
<summary><code>sum_hour_group1</code></summary>

```sql
SELECT
  tumbleStart(order_by_extended_time_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(order_by_extended_time_events.time, toIntervalHour(1), 'UTC') AS windowend,
  sum(toDecimal128OrNull(toString(order_by_extended_time_events.data.value), 19)) AS value,
  order_by_extended_time_events.subject AS subject,
  toString(order_by_extended_time_events.data.group1.:String) AS group1
FROM order_by_extended_time_events
WHERE order_by_extended_time_events.namespace = {namespace:String}
  AND order_by_extended_time_events.type = {type:String}
  AND order_by_extended_time_events.subject IN {subjects:Array(String)}
  AND order_by_extended_time_events.time >= {from:DateTime}
  AND order_by_extended_time_events.time < {to:DateTime}
  AND toString(order_by_extended_time_events.data.group1.:String) = {group1:String}
GROUP BY windowstart, windowend, subject, group1
ORDER BY windowstart
SETTINGS optimize_move_to_prewhere = 1, allow_reorder_prewhere_conditions = 1
```

</details>

<details>
<summary><code>sum_hour_group1_group2</code></summary>

```sql
SELECT
  tumbleStart(order_by_extended_time_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(order_by_extended_time_events.time, toIntervalHour(1), 'UTC') AS windowend,
  sum(toDecimal128OrNull(toString(order_by_extended_time_events.data.value), 19)) AS value,
  order_by_extended_time_events.subject AS subject,
  toString(order_by_extended_time_events.data.group1.:String) AS group1,
  toString(order_by_extended_time_events.data.group2.:String) AS group2
FROM order_by_extended_time_events
WHERE order_by_extended_time_events.namespace = {namespace:String}
  AND order_by_extended_time_events.type = {type:String}
  AND order_by_extended_time_events.subject IN {subjects:Array(String)}
  AND order_by_extended_time_events.time >= {from:DateTime}
  AND order_by_extended_time_events.time < {to:DateTime}
  AND toString(order_by_extended_time_events.data.group1.:String) = {group1:String}
  AND toString(order_by_extended_time_events.data.group2.:String) = {group2:String}
GROUP BY windowstart, windowend, subject, group1, group2
ORDER BY windowstart
SETTINGS optimize_move_to_prewhere = 1, allow_reorder_prewhere_conditions = 1
```

</details>

<details>
<summary><code>sum_hour_group1_group2_no_prewhere</code></summary>

```sql
SELECT
  tumbleStart(order_by_extended_time_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(order_by_extended_time_events.time, toIntervalHour(1), 'UTC') AS windowend,
  sum(toDecimal128OrNull(toString(order_by_extended_time_events.data.value), 19)) AS value,
  order_by_extended_time_events.subject AS subject,
  toString(order_by_extended_time_events.data.group1.:String) AS group1,
  toString(order_by_extended_time_events.data.group2.:String) AS group2
FROM order_by_extended_time_events
WHERE order_by_extended_time_events.namespace = {namespace:String}
  AND order_by_extended_time_events.type = {type:String}
  AND order_by_extended_time_events.subject IN {subjects:Array(String)}
  AND order_by_extended_time_events.time >= {from:DateTime}
  AND order_by_extended_time_events.time < {to:DateTime}
  AND toString(order_by_extended_time_events.data.group1.:String) = {group1:String}
  AND toString(order_by_extended_time_events.data.group2.:String) = {group2:String}
GROUP BY windowstart, windowend, subject, group1, group2
ORDER BY windowstart
```

</details>

<details>
<summary><code>sum_hour_group1_no_prewhere</code></summary>

```sql
SELECT
  tumbleStart(order_by_extended_time_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(order_by_extended_time_events.time, toIntervalHour(1), 'UTC') AS windowend,
  sum(toDecimal128OrNull(toString(order_by_extended_time_events.data.value), 19)) AS value,
  order_by_extended_time_events.subject AS subject,
  toString(order_by_extended_time_events.data.group1.:String) AS group1
FROM order_by_extended_time_events
WHERE order_by_extended_time_events.namespace = {namespace:String}
  AND order_by_extended_time_events.type = {type:String}
  AND order_by_extended_time_events.subject IN {subjects:Array(String)}
  AND order_by_extended_time_events.time >= {from:DateTime}
  AND order_by_extended_time_events.time < {to:DateTime}
  AND toString(order_by_extended_time_events.data.group1.:String) = {group1:String}
GROUP BY windowstart, windowend, subject, group1
ORDER BY windowstart
```

</details>

<details>
<summary><code>sum_hour_tz</code></summary>

```sql
SELECT
  tumbleStart(order_by_extended_time_events.time, toIntervalHour(1), 'America/New_York') AS windowstart,
  tumbleEnd(order_by_extended_time_events.time, toIntervalHour(1), 'America/New_York') AS windowend,
  sum(toDecimal128OrNull(toString(order_by_extended_time_events.data.value), 19)) AS value,
  order_by_extended_time_events.subject AS subject
FROM order_by_extended_time_events
WHERE order_by_extended_time_events.namespace = {namespace:String}
  AND order_by_extended_time_events.type = {type:String}
  AND order_by_extended_time_events.subject IN {subjects:Array(String)}
  AND order_by_extended_time_events.time >= {from:DateTime}
  AND order_by_extended_time_events.time < {to:DateTime}
GROUP BY windowstart, windowend, subject
ORDER BY windowstart
```

</details>

<details>
<summary><code>sum_minute</code></summary>

```sql
SELECT
  tumbleStart(order_by_extended_time_events.time, toIntervalMinute(1), 'UTC') AS windowstart,
  tumbleEnd(order_by_extended_time_events.time, toIntervalMinute(1), 'UTC') AS windowend,
  sum(toDecimal128OrNull(toString(order_by_extended_time_events.data.value), 19)) AS value,
  order_by_extended_time_events.subject AS subject
FROM order_by_extended_time_events
WHERE order_by_extended_time_events.namespace = {namespace:String}
  AND order_by_extended_time_events.type = {type:String}
  AND order_by_extended_time_events.subject IN {subjects:Array(String)}
  AND order_by_extended_time_events.time >= {from:DateTime}
  AND order_by_extended_time_events.time < {to:DateTime}
GROUP BY windowstart, windowend, subject
ORDER BY windowstart
```

</details>

<details>
<summary><code>sum_month</code></summary>

```sql
SELECT
  toDateTime(tumbleStart(order_by_extended_time_events.time, toIntervalMonth(1), 'UTC'), 'UTC') AS windowstart,
  toDateTime(tumbleEnd(order_by_extended_time_events.time, toIntervalMonth(1), 'UTC'), 'UTC') AS windowend,
  sum(toDecimal128OrNull(toString(order_by_extended_time_events.data.value), 19)) AS value,
  order_by_extended_time_events.subject AS subject
FROM order_by_extended_time_events
WHERE order_by_extended_time_events.namespace = {namespace:String}
  AND order_by_extended_time_events.type = {type:String}
  AND order_by_extended_time_events.subject IN {subjects:Array(String)}
  AND order_by_extended_time_events.time >= {from:DateTime}
  AND order_by_extended_time_events.time < {to:DateTime}
GROUP BY windowstart, windowend, subject
ORDER BY windowstart
```

</details>

<details>
<summary><code>sum_no_window</code></summary>

```sql
SELECT
  tumbleStart(min(order_by_extended_time_events.time), toIntervalMinute(1), 'UTC') AS windowstart,
  tumbleEnd(max(order_by_extended_time_events.time), toIntervalMinute(1), 'UTC') AS windowend,
  sum(toDecimal128OrNull(toString(order_by_extended_time_events.data.value), 19)) AS value,
  order_by_extended_time_events.subject AS subject
FROM order_by_extended_time_events
WHERE order_by_extended_time_events.namespace = {namespace:String}
  AND order_by_extended_time_events.type = {type:String}
  AND order_by_extended_time_events.subject IN {subjects:Array(String)}
  AND order_by_extended_time_events.time >= {from:DateTime}
  AND order_by_extended_time_events.time < {to:DateTime}
GROUP BY subject
```

</details>

<details>
<summary><code>unique_count_hour</code></summary>

```sql
SELECT
  tumbleStart(order_by_extended_time_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(order_by_extended_time_events.time, toIntervalHour(1), 'UTC') AS windowend,
  uniqExact(nullIf(toString(order_by_extended_time_events.data.value.:Float64), 'null')) AS value,
  order_by_extended_time_events.subject AS subject
FROM order_by_extended_time_events
WHERE order_by_extended_time_events.namespace = {namespace:String}
  AND order_by_extended_time_events.type = {type:String}
  AND order_by_extended_time_events.subject IN {subjects:Array(String)}
  AND order_by_extended_time_events.time >= {from:DateTime}
  AND order_by_extended_time_events.time < {to:DateTime}
GROUP BY windowstart, windowend, subject
ORDER BY windowstart
```

</details>

<details>
<summary><code>workload_seconds_by_region</code></summary>

```sql
SELECT
  tumbleStart(order_by_extended_time_events.time, toIntervalHour(1), 'UTC') AS windowstart,
  tumbleEnd(order_by_extended_time_events.time, toIntervalHour(1), 'UTC') AS windowend,
  sum(toDecimal128OrNull(toString(order_by_extended_time_events.data.duration_seconds), 19)) AS value,
  toString(order_by_extended_time_events.data.region.:String) AS region
FROM order_by_extended_time_events
WHERE order_by_extended_time_events.namespace = {namespace:String}
  AND order_by_extended_time_events.type = 'workload'
  AND order_by_extended_time_events.subject IN {subjects:Array(String)}
  AND order_by_extended_time_events.time >= {from:DateTime}
  AND order_by_extended_time_events.time < {to:DateTime}
GROUP BY windowstart, windowend, region
ORDER BY windowstart
```

</details>
